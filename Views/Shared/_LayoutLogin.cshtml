@{
    var actionAtual = ViewContext.RouteData.Values["action"] != null ? ViewContext.RouteData.Values["action"].ToString() : "login";
}

<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="~/css/reset.css">
    <link rel="stylesheet" href="~/css/default.css">
    <link rel="stylesheet" href="~/css/navbar.css">
    <link rel="stylesheet" href="~/css/login-signup/style.css" />
    <title>Aion | Login</title>
</head>

<body>
    <header>
        <nav class="navbar">
            <a class="logo-nav" asp-action="Index" asp-controller="Home">
                <img src="~/img/logo/aion-white.png" alt="Logo" />
            </a>
            <input type="checkbox" id="menu-hamburger">
            <label id="btn-menu" for="menu-hamburger">
                <img src="~/img/icons/menu-hamburger.svg" alt="Menu">
            </label>
            <ul class="nav-items-container">
                <li><a class="nav-item-white @(actionAtual == "Index" ? "selected" : "")" asp-action="Index" asp-controller="Home">Início</a></li>
                <li><a class="nav-item-white @(actionAtual == "Sobre" ? "selected" : "")" asp-action="Sobre" asp-controller="Home">Sobre</a></li>
                <li><a class="nav-item-white @(actionAtual == "Planos" ? "selected" : "")" asp-action="Planos" asp-controller="Home">Planos</a></li>
                <li><a class="nav-item-white @(actionAtual == "Privacidade" ? "selected" : "")" href="javascript:void(0)">Privacidade</a></li>
                <button class="btn" id="btn-hamburger">Login</button>
            </ul>
            <a href="#" class="btn hidden-btn" id="btn-nav">Login</a>
        </nav>
    </header>

    <main>
        @RenderBody()
    </main>

    @await RenderSectionAsync("Scripts", required: false)

</body>

</html>